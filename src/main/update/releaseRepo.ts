/**
 * Telos 更新所指向的 GitHub 仓库坐标（唯一事实来源）。
 * 与 src/shared/updateSources.ts 保持一致。
 */
export const UPDATE_REPO_OWNER = "gygy";
export const UPDATE_REPO = "telos";

export const RELEASES_URL = `https://github.com/${UPDATE_REPO_OWNER}/${UPDATE_REPO}/releases`;
